import { buttonStyles, BUTTON_THEME_KEY } from 'components/Base/Button/styles';
import {
  headingStyles,
  HEADING_THEME_KEY,
} from 'components/Base/Heading/styles';

export const components = {
  [HEADING_THEME_KEY]: headingStyles,
  [BUTTON_THEME_KEY]: buttonStyles,
};
